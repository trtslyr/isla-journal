export { database } from './db'
export type { FileRecord, SearchIndex, ChatRecord, ChatMessageRecord, AppSettings } from './db'

// Re-export for convenience
export { database as default } from './db' 